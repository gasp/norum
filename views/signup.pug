extends layout

block content
  h1 signup
  p 1/3
  form(method="post", action="/signup")
    p create a new account to get assigned a password
    if error
      div.error= error
    div
      label username
        input(type="text", name="login", autofocus, value=login)
    input(type="submit",value="next")
